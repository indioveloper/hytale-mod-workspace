# Trigger Remove Event Title

Adds a `RemoveEventTitle` effect to Hytale Trigger Volumes.

The effect hides the current event title for the player that triggered the volume. `FadeOutDuration` controls how long the title takes to disappear.
